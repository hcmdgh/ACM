#include <iostream>
#include <string>
using namespace std;

int N;
string T, S[200005];

int main() {
    ios::sync_with_stdio(false);
    cin >> T >> N;
    for (int i = 0; i < N; ++i) {
        cin >> S[i];
    }

    return 0;
}
