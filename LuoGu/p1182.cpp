#include <iostream>
using namespace std;

int N, M;
int nums[100000];

int main() {
    ios::sync_with_stdio(false);
    cin >> N >> M;
    for (int i = 0; i < N; ++i) {
        cin >> nums[i];
    }

    return 0;
}