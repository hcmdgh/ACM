#include <iostream>
using namespace std;

char N[205];
int S, K;

int main() {
#ifdef DEBUG
    freopen("in.txt", "r", stdin);
#endif
    while (scanf("%s%d%d", N, &S, &K) == 3) {

    }

    return 0;
}